<div class="container">
  <p id="notice"><%= notice %></p>

  <p>
  <strong>Title:</strong>
  <%= @service.title %>
  </p>

  <p>
  <strong>Description:</strong>
  <%= @service.description %>
  </p>

  <p>
  <strong>Walker:</strong>
  <%= @service.walker.walker_name %>
  </p>
</div>

<% if @service.walker == current_user %>
  <%= link_to 'Edit', edit_service_path(@service) %> |
<% end %>

<%= link_to 'Back', services_path %>
